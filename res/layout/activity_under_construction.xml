<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/blue"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:orientation="vertical" >

    <!-- Layout zur UnderConstructionActivity (Activities, die noch in Entwicklung sind) -->
    
    <!-- Ueberschrift der Under-Construction-Activity -->
    <TextView
        android:id="@+id/title_construction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:textSize="22sp"
        android:text="@string/title_construction" />

    <!-- Trennlinie Start -->
    <TextView
        android:layout_width="fill_parent"
        android:layout_marginTop="2dp"
        android:layout_marginBottom="18dp"
        android:layout_height="2dip"
        android:background="#FF000000" />
    <!-- Trennlinie Ende -->
    
    <!-- Hinweis -->
    <TextView
        android:id="@+id/text_construction"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:text="@string/text_construction" >
	</TextView>
	
    <!-- Bild aus res/drawable -->
    <ImageView
		android:id="@+id/underconstruction"
		android:layout_width="wrap_content" 
		android:layout_height="wrap_content"
		android:layout_below="@+id/text_construction"
		android:src="@drawable/underconstruction" />

</LinearLayout>